# gh auth
alias gha='gh auth'

# gh browse
alias ghb='gh browse'
alias ghbs='gh browse --settings'

# gh config
alias ghc='gh config'

# gh issue
alias ghi='gh issue'

# gh pr
alias ghpr='gh pr'
alias ghprc='gh pr create'
alias ghprm='gh pr merge'

# gh release
alias ghr='gh release'

# gh repo
alias ghrepoc='gh repo create'

# gh secret
alias ghs='gh secret'
alias ghsd='gh secret delete'
alias ghsl='gh secret list'
alias ghss='gh secret set'

# gh workflow
alias ghwf='gh workflow'
alias ghwfr='gh workflow run'
